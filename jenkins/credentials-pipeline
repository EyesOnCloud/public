pipeline {
    agent any

    stages {

        stage('Build') {
            steps {
                echo "Build stage - no credentials needed here"
                sh 'mkdir -p output && echo "artifact" > output/app.jar'
            }
        }

        stage('Integration Test') {
            steps {
                echo "Integration test stage"
                withCredentials([
                    usernamePassword(
                        credentialsId: 'database-credentials',
                        usernameVariable: 'DB_USER',
                        passwordVariable: 'DB_PASS'
                    )
                ]) {
                    echo "Connecting to test DB as ${DB_USER}"
                    sh '''
                        if [ -n "$DB_PASS" ]; then
                            echo "DB auth: OK"
                        else
                            echo "DB auth: FAILED - no password"
                            exit 1
                        fi
                    '''
                    echo "Test DB connection established"
                }
                echo "After withCredentials - DB_USER is: ${DB_USER}"
            }
        }

        stage('Deploy') {
            steps {
                echo "Deploy stage"
                withCredentials([
                    usernamePassword(
                        credentialsId: 'database-credentials',
                        usernameVariable: 'DB_USER',
                        passwordVariable: 'DB_PASS'
                    ),
                    string(
                        credentialsId: 'api-token',
                        variable: 'API_KEY'
                    )
                ]) {
                    echo "Deploying as ${DB_USER}"
                    sh 'echo "API call with token: $API_KEY"'
                    sh 'echo "artifact" | cat output/app.jar'
                    echo "Deploy complete"
                }
            }
        }

    }

    post {
        always {
            sh 'rm -rf output/'
            echo "Cleanup done"
        }
    }
}
